{
  "name": "ssh-client",
  "version": "0.0.20",
  "private": true,
  "description": "Asset pipeline for ssh-client",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/css/app.css -o ./priv/static/css/app.css --minify"
  },
  "devDependencies": {
    "daisyui": "^4.12.14",
    "tailwindcss": "^3.4.17"
  }
}
